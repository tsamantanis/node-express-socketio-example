## NodeJS Express Socket.io Example

To install dependencies run:
```
npm install
```

To start the development server run:
```
npm start
```

Head over to `http://localhost:5000` to view the example chat app